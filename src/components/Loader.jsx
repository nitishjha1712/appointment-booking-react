import React from "react";
import "./../spinner.css";
const Loader = (props) => {
    return (
        <div className="loader-container">
            <div className="spinner"></div>
        </div>
    )
};

export default Loader;